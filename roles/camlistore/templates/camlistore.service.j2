[Unit]
Description=camlistore
After=mysql.service
Requires=mysql.service

[Service]
User={{ansible_user}}
ExecStart=/home/{{ansible_user}}/go/src/camlistore/bin/camlistored
RestartSec=1s
Restart=always

[Install]
WantedBy=multi-user.target
